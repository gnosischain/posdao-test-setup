#!/bin/bash

# Exit on undefined variables.
set -u
set -e
set -x

NETHERMIND=$1
for i in $(seq 0 6); do
    dotnet "$NETHERMIND" --config "./config/node${i}.cfg" >> "./data/node${i}/log" 2>&1 &
    node ./scripts/getReservedPeer.js "$i"
done
